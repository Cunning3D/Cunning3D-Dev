name = "Create Cat (VOX)"
category = "Test Assets"
description = "Creates the built-in cat voxel asset and outputs a voxel-backed Geometry."

[io]
input_type = "None"
output_type = "Geometry"

[[io.outputs]]
index = 0
name = "Output"
description = "Voxel-backed geometry referencing an internal voxel grid."

[usage]
text = """
Generator node: no inputs.
Use this node for quick viewport sanity checks and color palette validation using a small built-in voxel character asset.
"""

