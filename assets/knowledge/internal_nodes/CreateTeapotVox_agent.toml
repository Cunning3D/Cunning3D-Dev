name = "Create Teapot (VOX)"
category = "Test Assets"
description = "Creates the built-in teapot voxel asset and outputs a voxel-backed Geometry."

[io]
input_type = "None"
output_type = "Geometry"

[[io.outputs]]
index = 0
name = "Output"
description = "Voxel-backed geometry referencing an internal voxel grid."

[usage]
text = """
Generator node: no inputs.
Use this node to quickly validate the voxel rendering and meshing pipeline with a deterministic built-in teapot asset.
"""

