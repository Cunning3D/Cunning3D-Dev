// Shared gizmo constants (centralized to avoid magic numbers scattered across code).

pub const SPLINE_HANDLE_SIZE_FACTOR: f32 = 0.12;
pub const SPLINE_HANDLE_SIZE_MIN: f32 = 0.15;
pub const SPLINE_RING_SEGMENTS: usize = 64;
pub const SPLINE_HANDLE_PICK_DISTANCE_PX: f32 = 10.0;
