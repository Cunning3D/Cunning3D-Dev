use crate::libs::geometry::mesh::Geometry;

/// Split edges longer than a target length.
/// Note: Implementation is currently in other passes; this module exists for stable API wiring.
pub fn split_edges(_geo: &mut Geometry, _target_length: f32) {
    // TODO: implement (kept intentionally empty to satisfy module wiring without changing algorithms elsewhere)
}

