//! Edge collapse operation for isotropic remesh.
use crate::libs::geometry::mesh::Geometry;

pub fn collapse_edges(geo: &mut Geometry, target_length: f32) {
    let _ = (geo, target_length); // Placeholder
}
