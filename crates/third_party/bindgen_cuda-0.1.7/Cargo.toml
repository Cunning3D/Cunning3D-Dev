[package]
name = "bindgen_cuda"
version = "0.1.7"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Patched bindgen_cuda to force MSVC /MD runtime for nvcc host compilation."

[dependencies]
glob = "0.3"
num_cpus = "1.16"
rayon = "1.10"
